@@preprocessing
<%
function hasPhone(items) {
  return Object.keys(items || {}).some(k => /手机|手环|电话|通讯/.test(k));
}
let anyPhone = hasPhone(getvar('stat_data.主角.随身物品', { defaults: {} }));
if (!anyPhone) {
  const roster = getvar('stat_data.角色名录', { defaults: {} }) || {};
  anyPhone = Object.keys(roster).some(n => hasPhone(roster[n] && roster[n].随身物品));
}
if (anyPhone || matchChatMessages(['手机', '微信', '短信', '来电', '发消息'])) {
  print(await getwi('手机消息格式'));
}
%>
