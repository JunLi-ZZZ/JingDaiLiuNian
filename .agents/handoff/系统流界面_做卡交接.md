# 系统流界面 → 做卡会话交接

> 本文档由前端会话维护，每完成一块界面就更新。做卡会话请先读完再动手。
> 最近更新：2026-08-16 Codex 第四版存档（超常生物保全与观测局）；2026-08-11 的同名“第四轮”历史完整保留在下文。

## 分工边界

- **前端会话（本文档作者）**：只做界面 + mock 数据 + 程序逻辑，浏览器直接预览，不进酒馆。
- **做卡会话（你）**：定 schema、把界面接进 MVU 变量、写 AI 规则、决定挂载方式。

## 已完成清单

| 界面 | 位置 | 状态 |
|---|---|---|
| 通用主状态栏 | `src/系统流/界面/状态栏/` | ✅ 完成（等级/六维/生命能量/技能/背包/任务/人物/装备/宿主） |
| 神兽动物园 DLC | `src/系统流/界面/动物园/` | ✅ 完成（兽员/图鉴/召唤/孵化/食物/商店/设置七 Tab） |
| 共享品阶工具 | `src/系统流/界面/shared/tier.ts` + `TierTag.vue` | ✅ 十档品阶色 + tierRank 排序 |

### 动物园第二轮完善（2026-08-09）

- **兽员个体命名**：record 键=个体名字（非种族名），同名种族可养多只；抽到/孵出自动起名（NAME_POOLS 预览用，正式版 AI 起名）；卡片背面点击名字可改名
- **图鉴 Tab**：按类别分组的收集进度（X/18），未收集显示剪影「？？？」
- **自定义许愿池**：召唤页可新建卡池（池名/内容类型/类别筛选/主题描述/价格），概率保底沿同内容内置池默认值，可在设置页改；localStorage v2（`dlc_zoo_pool_settings_v2`）持久化，自建池完整结构也存
- **创造模式（设置页）**：愿力/园区等级/游客上限直接改、全体喂饱、手动造兽表单（预览版手填描述，正式版 AI 补全）
- **抚摸互动**：兽娘卡背面，+3 心情

### 动物园第三轮改版（2026-08-10）

- **视觉自然风重构**：用户明确"每个 DLC 不能只换主题色"。动物园改为深林底+羊皮纸卡片（米白卡面/深棕文字/木质园牌/拱门兽栏位/有机圆角/楷体标题），与主状态栏的科技全息风结构性区分。配色变量分两套：铬件区浅色文字（`--txt*`）+ 纸卡区深色文字（`--ptxt*`/`--pinset`/`--pline`），见 [global.css](src/系统流/界面/动物园/global.css)
- **商店货架制**：`SHOP_POOL` 商品池，货架随机铺 4 格，30 愿力手动刷新；限购商品（建设类）卖完标记"已售罄"
- **新建设商品（游戏内成长功能）**：异种展馆（解锁异种类别，限购1）/祈灵祭坛（召唤池高品阶+，限购3）/灵植温室（食材池高品阶+，限购2）。限购计数在 `data.建设记录`，概率加成购买即写入卡池概率并存档
- **异种锁定**：`data.解锁异种=false` 时兽员池/图鉴过滤异种（初始蛋"沉眠之卵"是特例礼物）；头部显示锁定提示
- **创造模式上锁**：设置页默认锁定屏，连续两次确认才解锁（会话内有效，刷新页面重新上锁）——用户原话"不是所有人都喜欢简单模式"

### 动物园第四轮改版（2026-08-11）

**用户反馈**："不要科技风也不要单纯自然风。动物园兼容奇幻/西方魔幻/东方修仙/上古洪荒神兽，需要威严、神秘、史诗感，不能幼稚。"

**新方向**："神话档案馆 × 禁忌典籍" —— 不是经营动物园，而是管理一座收录传说生物的秘密档案库（大英博物馆密室 + 梵蒂冈机密档案 + 炼金术师工坊）。

**本轮改动**（配色系统重构为主，纹理/装饰/形态层深化待后续）：
- **底色换深紫褐夜空**：`#1a0f1e` → `#2a1b2e`（古旧皮革装订/深夜星空，不再是绿色森林）
- **卡片做旧泛黄**：羊皮纸改为 `#e8d7b8`，边缘加烧灼暗角（`radial-gradient` 叠加四角）
- **装饰色古铜金箔**：古铜锈迹 `#8b6f47` + 深金箔 `#d4af37`（像古籍装帧，不再是明快绿橙）
- **品阶宝石化**：从饱和明色改为深邃半透宝石色（深红宝石 `#8b0000`、蓝宝石 `#1e3a5f`、祖母绿 `#0f4d3a`、黑曜石等）
- **园牌炼金术圆环化**：六芒星魔法阵背景 + 镂空古文圆环 + 中心园名（`font-variant: small-caps` 花体，`letter-spacing: 8px`）
- **Tab 书脊风格**：竖版书脊标签（左侧垂直），选中态深金箔边框 + 浮雕质感
- **卡片烫金双边框**：外层古铜、内层金箔；兽员立绘椭圆画框式
- **类别图标线刻化**：五类生物改为单色线刻浮雕风格（古铜色 + 微弱发光，像古代壁画）
- **按钮印章封蜡质感**：圆角矩形 + 内嵌高光，hover 时加深金箔光晕
- **字体衬线化**：标题启用 `font-variant: small-caps`（小型大写字母），增强档案仪式感

**评价**：跨越东西方神话的通用古籍语言（烫金 + 深色皮革），史诗感 + 禁忌感，不再幼稚但保留手工质感。

## 预览机制（做卡会话也要懂）

- 状态栏 [store.ts](src/系统流/界面/状态栏/store.ts) 有**环境自检**：`typeof getCurrentMessageId === 'function'` 在酒馆里走 `defineMvuDataStore`，纯浏览器走 mock。组件全部只读 `store.data.*`，零硬编码。
- 各界面 `index.html` 带**条件 CDN 引导**：检测到缺 `Vue`/`_`/`z` 全局时自动从 jsDelivr 补（酒馆里已有全局则完全跳过）。zod4 无 UMD，用 ESM module 补，时序已验证。
- 本地预览：`pnpm build` → 起静态服务器（`serve.js` 8080 或任意端口）→ 开 `dist/系统流/界面/<界面名>/index.html`。
- 产物校验：`grep -q "__webpack_require__" dist/.../index.html` 有输出=残缺，正常应无。

## 动物园数据契约（schema 雏形）

[types.ts](src/系统流/界面/动物园/types.ts) 是定 schema 的底稿。要点：

- **挂法建议**：主 schema 加顶层字段 `动物园`（object），结构按 `动物园数据` 接口。
- **兽员**：record，键=名称。双形态文本（`兽形`/`兽娘`）都有；`立绘兽形`/`立绘兽娘` 字段已预留（空则用品阶纹章占位），生图走 gpt-image-assets skill。
- **类别**五档：神兽/凶兽/幻兽/魔兽/异种（配色图标在 [category.ts](src/系统流/界面/动物园/category.ts)）。用户明确：不止东方神兽，西方三头犬/耶梦加得、克苏鲁系不可名状都收。
- **货币=愿力**：游客参观产出，用于抽奖/灌注/购物。
- **卡池三个内置**（兽员召唤/兽蛋祈愿/食材补给）+**用户自建许愿池**，概率与保底**全部程序控制**（[store.ts](src/系统流/界面/动物园/store.ts)），设置页可调（存 localStorage `dlc_zoo_pool_settings_v2`，含自建池完整结构）。**AI 只写描述，绝不能让 AI 决定出货**——防言出法随。
- 抽卡/喂食/抚摸/改名/孵化/购物/货架刷新/限购/解锁异种/概率加成建筑逻辑已在 store 实现（同名种族可养多只，个体以名字区分；孵化位 4 格；灌注 100 愿力/次；货架 4 格刷新 30 愿力）。

## 做卡会话待办

1. **schema**：加 `动物园` 字段（照 types.ts），zod 化时注意 record/object 默认值。
2. **store 换源**：动物园 store.ts 顶部注释有说明——把 `data` 换成 `defineMvuDataStore`，逻辑函数保留。
3. **挂载方式**：参考镜待流年 PhonePanel（状态栏 iframe 内 Teleport 到父窗口 body 做全屏浮层，见 memory `手机浮层面板技术`）。
4. **正文卡片**：用户设想——兽员/食物/兽蛋在正文里像镜待流年的生物/物品卡片一样正则渲染（见 memory `辅助内容与正文的关系`），面板本体不受影响。
5. **AI 边界**：抽奖概率、保底、变异等数值规则全在程序侧；AI 只负责生成文本描述（如新兽员的双形态描述），新兽员可追加进兽员 record 并可混入抽奖候选。
6. **游客准入设定**：用户提过"仅限美丽女性游客参观"的世界观限制——这是卡设定/世界书层面的事，界面无关。

## 用户提过的后续方向（未做）

- **变异系统**：两只同族合成/特定食物触发变异，store 加规则函数即可，界面卡片结构兼容。
- 其他 DLC 线：百倍返现/天赋进化/三合一合成（各自再做专属界面）。

## 构建与升版注意

- 状态栏 dist 已构建但**尚未进卡**（预览阶段，index.yaml 未挂）。
- 升版必须 `node bump_version.mjs vXXX`（见 memory `bump_version_script`）；多卡体系适配见 memory `多卡体系-架构与转换`。
- git 提交范围：只交卡区/src/dist 对应文件，别 `git add -A`（见 memory `feedback_git提交范围`）。

## 存档与当前状态（2026-08-12 做卡会话补记）

**已存档**：commit `ee849701`，含 `src/系统流` + `dist/系统流` + `.agents/handoff` 共 59 文件。三个 dist HTML 均已校验无 `__webpack_require__` 残缺。前端会话的前两版曾因未及时提交而丢失（只剩第三版），此后每完成一版即 git。

**GitHub 仓库不用新建**：继续用现有 `JunLi-ZZZ/JingDaiLiuNian`。jsDelivr 按路径取文件，仓库名与卡名无关，CDN 路径为 `.../gh/JunLi-ZZZ/JingDaiLiuNian/dist/系统流/界面/<界面名>/index.html`。新建仓库反而要重配 CDN 并重等首次同步。

**镜待流年不受影响**：webpack 各编各的，两卡在同一仓库并存无冲突。唯一交叉点是 `package.json` 的 postbuild 和 `bump_version.mjs`，那两个目前是镜待流年专用，系统流不跑就不干扰。

**src 与卡区的关系**：两者内容应完全一致（已验证镜待流年 状态栏/封面/MirrorPanel/schema 四文件全同）。同步用 `cp -r "src/系统流/界面" "卡区/系统流/"` 整份复制，**不要逐行编辑**——费 token 且容易两边漂移。建议等接 MVU 时一并同步，因动物园 `store.ts` 届时必然从 mock 改为 `defineMvuDataStore`。

**升版脚本改造方向**（等系统流真要发版时再做）：不必每卡一份脚本。一份脚本 + 按卡查表即可，命令形如 `node bump_version.mjs 系统流 v2`。镜待流年保留现有 12 项替换（含 MirrorPanel 的 wbName、卡区/src 双副本、CDN URL）；系统流若不做镜渡，只需 3 项（postbuild 路径、tavern_sync 卡名、index.yaml 的 CDN URL）。不带卡名参数时默认镜待流年，保持现有习惯不变。

**合集卡的运行负担**（已查证，供日后设计参考）：负担不在 schema 而在**变量状态**。`卡区/*/世界书/变量/变量列表.txt` 里的 `{{format_message_variable::stat_data}}` 每轮把实际有值的变量全量展开进提示词，而 zod 的 `prefault` 会把默认值**填实**（等级 1、属性各 10、生命 100），不是空对象。所以若让两条线的字段并存，玩镜待流年时每轮也要白带系统流那堆无关数字。用户据此决定：**先做纯新卡，日后融合时再做整体切换**，切线应清掉另一条线的变量而非常驻。规则层面的按线注入已有现成范式——`EJS控制器` 就是按 `初始化完成` 二选一注入首次/日常更新规则（两份各 12-13KB，只注一份）。

**项目节奏**：2026-08-12 起系统流暂停。当初赶做是因 coding 套餐将到期，套餐已用得差不多。用户转回先完善镜待流年（小手机转发/注入/细节），系统流新内容待镜待流年收尾后再开。

## 2026-08-16 Codex 第四版存档补记

### 权威定位

- 第四版前端代码提交：`6669d22a`（`feat: 系统流动物园第四版保全局界面`）。
- 上一版整线存档：`ee849701`。前者只改动物园表现层和对应 dist，后者仍是整条系统流的完整基线。
- 版本编号有重名：2026-08-11 古籍皮革稿在旧会话中叫“第四轮”，2026-08-16 用户把新稿称为“第四版”。后续讨论必须使用“日期 + 视觉名 + commit”，不要用“第四版”单独指代。

### 用户确认与设计结论

- 用户评价本版“还不错”，明确要求 Git 存档、更新记忆进程和交接文档。
- 新方向是“超常生物保全与观测局”，强调成熟、克制、机构化管理感；炭黑为底，氧化铜青表示正常状态，暗红表示管制风险，黄铜只作资源/品阶重点。
- 旧版羊皮纸、椭圆空画框、深紫褐古籍、封蜡按钮被移除。桌面为左侧馆藏索引 + 两列档案，移动端为横向导航 + 单列档案。
- 七个 Tab 和原业务逻辑全部保留。`store.ts`、`types.ts`、mock、概率、保底、孵化、喂食、商店逻辑均未改；本轮不能被描述为“已接 MVU”。

### 文件与验证证据

- 主要改动：`src/系统流/界面/动物园/App.vue`、`components/ZooHeader.vue`、`global.css`、`index.ts`，新增 `v4.css`；production 产物同步更新到 `dist/系统流/界面/动物园/`。
- 已在 1100px 桌面与 390px 手机视口检查；无页面级横向溢出，手机兽员卡翻面后的抚摸/喂食按钮未越界。
- 已逐页检查孵化、补给、采购、管制，实测单抽会扣除愿力并打开暗色 Teleport 结果弹窗；浏览器控制台无 warning/error。
- ESLint 和 Prettier 通过；目标 production HTML 已检查无 `__webpack_require__` 残缺。
- `pnpm run build:dev` 成功。整体 `pnpm build` 在所有 webpack 入口成功后，仍会被镜待流年专用 `postbuild` 复制命令以 `3221226505` 拉成非零退出；动物园产物已生成。这个失败属于现有跨卡构建脚本问题，不属于第四版页面运行错误。

### 尚未完成，后续不要误报

- 尚未把 `src/系统流` 同步到 `卡区/系统流`。
- 尚未把动物园 store 接到 `defineMvuDataStore`，也没有把 `动物园` 顶层字段正式加入 schema。
- 尚未挂载到系统流 `index.yaml` 或正式卡，因此当前仍是浏览器 mock 原型。
- AI 与程序边界仍按上文执行：概率、保底和数值规则由程序控制，AI 只负责符合既有事实的文本描述。

### 记忆接续

- 主 `MEMORY.md` 截至本次检查没有完整的系统流前端历史；只有构建、多卡和交接核验原则，且一条临时名称乱码。
- 第四版说明已写入 `memories/extensions/ad_hoc/notes/20260816-205910-system-flow-zoo-v4.md`，但该目录内容要等 Codex 后台汇总后才可能进入主记忆。新会话不得只搜主记忆后就断言没有第四版；必须读本文件、`系统流前端交接.md`、`git log` 和当前源码。
- 保留此前所有轮次、数据契约、做卡待办和存档说明，不因当前版本更新而删减，避免新会话失去来龙去脉。

## 2026-08-16 Codex 通用状态栏预览精修补记

- 通用状态栏仍是中性科技底座；本轮仅做预览启动稳定性、长文本响应式和可读性修复。
- `R18模式` 在顶栏显示为只读徽标，前端不提供开关，也不触碰 EJS/世界书加载逻辑。
- 预览启动器会先补齐浏览器缺失的 `zod` / `lodash` 全局，再加载 Vue App；酒馆环境仍走既有全局与 `defineMvuDataStore` 分支。
- 390px、320px 视口无页面级横向溢出；六个状态栏分页均已在内置浏览器点击确认可渲染。
- 状态栏代码与 dist 已存档于 `d44265b7`（`feat: 优化系统流通用状态栏预览`）；开发构建包含正常内联 webpack runtime，不应只凭 `__webpack_require__` 字符串判定残缺。
- 本轮仍未同步 `卡区/系统流`、未接入正式 MVU/schema、未挂载 `index.yaml`，不要把浏览器 mock 误认为卡内联动完成。
